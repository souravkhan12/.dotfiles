#include <bits/stdc++.h>
using namespace::std;

#define sim template<class T
#define fexc() ios_base::sync_with_stdio(false),cin.tie(nullptr)
#define FOR(i,a,b) for (int i = a ; i < b; ++i)
#define ROF(i,a,b) for (int i = (a)-1; i >= b ; i--)
#define rep(a) FOR(_,0,a)
#define all(v) v.begin(),v.end()
#define SZ(a) int(a.size())
#define trav(i,v) for (auto &i :v)
#define fix(prec) {cout << setprecision(prec) << fixed;}
#define getunique(v) {sort(all(v)); v.erase(unique(all(v)), v.end());}

sim> T ckmin(T a, T b) { return ( a < b ? a : b);}
sim> T ckmax(T a, T b) { return (a > b ? a : b);}
sim> using V = vector<T>;
sim> using P = pair<T,T>;

/* Points to remeber */ 
/* 1) Thing which Data structure can use */
/* 2) Thing more if get WA more than 2 time , Corner Case */ 

signed main() {
	fexc();
	return 0;
}

